plugins {
    id "java"
    id "application"
}

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    implementation('org.mashov.models:cities:1.0.0')
    implementation('org.mashov.models:shows:1.0.0')
}
